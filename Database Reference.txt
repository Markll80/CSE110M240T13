Parse.enableLocalDatastore(this);
ParseObject.registerSubclass(Professor.class);  //---------------
Parse.initialize(this);
ParseUser.enableAutomaticUser();
ParseACL defaultACL = new ParseACL();
ParseACL.setDefaultACL(defaultACL, true);

Classe Name: Professor
String name
int numRatings
int easiness
int helpfullness
int clarity
Array - comments   (each element is actually an objectID to a comment in Comment class )


Class Name: Comment
String comment
int numLikes
Array - UsersLiked   (each element is also an objectID to user's objectID)
String - ProfName (temporary as name of prof, might be changed to objectID of prof later)
